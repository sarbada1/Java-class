// create an interface 'Shape' which has a method to draw a shape, fill color and scale. Creae two classes Circle and rectangle that implements shape. Implements abstract methods from shape in both classes. create objects of both shapes and use them to access features of shape.

// package interface;

public interface Shape {

    void draw();
    void fillColor();
    void scale();


}
